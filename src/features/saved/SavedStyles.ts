import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff9c4',
    padding: 16,
  },
  videoContainer: {
    marginBottom: 20,
  },
  video: {
    width: '100%',
    height: 200,
    borderRadius: 8,
    backgroundColor: '#000',
  },
  emptyText: {
    marginTop: 40,
    textAlign: 'center',
    color: '#6b4f1d',
    fontSize: 16,
  },
})

